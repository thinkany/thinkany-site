var NHFullWidthImage = (function($) {
	'use strict';
    
    // Initialize dynamic block preview (editor).

    function init() {
        events();
    }

    function events() {

    }

	return {
		init:init
	};

}(jQuery));